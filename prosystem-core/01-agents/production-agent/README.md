# {{AGENT_TITLE}}

This directory is a generated Pro System production agent. `agent.yaml` is the machine-readable source; the Markdown files define the human-readable operating contract. Validate and test before production use.
