# Agent Collaboration

Declare allowed senders and receivers explicitly. Forward bounded context, never unrestricted conversation history. Every handoff uses the shared handoff schema and preserves approval state.
