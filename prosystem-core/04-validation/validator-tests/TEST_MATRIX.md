# Test Matrix

Automated unit tests cover:

1. valid repository acceptance;
2. required-file failure;
3. dependency-cycle failure;
4. unknown-dependency failure;
5. conflict failure;
6. secret-detection failure;
7. semantic-version failure;
8. production-agent mandatory dependencies;
9. mandatory repository control files;
10. security-baseline deny-by-default invariants.

The complete real template repository is also validated after unit tests.
